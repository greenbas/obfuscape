[gd_scene load_steps=5 format=2]

[ext_resource path="res://src/Games/Game.gd" type="Script" id=1]
[ext_resource path="res://src/Elements/Themes/Poco_Theme.tres" type="Theme" id=2]

[sub_resource type="GDScript" id=1]
script/source = "extends Timer

"

[sub_resource type="GDScript" id=2]
script/source = "extends ProgressBar


onready var timer = get_node(\"../Timer\")


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	value = (timer.time_left / globals.timer_start_value) * 100
"

[node name="Game" type="Node2D"]
script = ExtResource( 1 )

[node name="Timer" type="Timer" parent="."]
script = SubResource( 1 )

[node name="Timer Bar" type="ProgressBar" parent="."]
margin_left = 8.0
margin_top = 580.0
margin_right = 792.0
margin_bottom = 592.0
theme = ExtResource( 2 )
percent_visible = false
script = SubResource( 2 )
__meta__ = {
"_edit_use_anchors_": false
}
[connection signal="timeout" from="Timer" to="." method="_on_Timer_timeout"]
